# Vault Enterprise - Configuration Management

## Pre-requisites

### Install the needed dependencies

```shell
ansible-galaxy install -r requirements.yml
```

### Then run the playbook

```shell
ansible-playbook playbook.yml
```
